/** A frozen, shared empty array. */
export const array: readonly never[];
/** A frozen, shared empty object with a `null` prototype. */
export const nil: Readonly<Record<string, never>>;
/** A frozen, shared empty object. */
export const object: Readonly<Record<string, never>>;

/** A frozen, shared empty array. */
export declare const array: readonly never[];
/** A frozen, shared empty object with a `null` prototype. */
export declare const nil: Readonly<Record<string, never>>;
/** A frozen, shared empty object. */
export declare const object: Readonly<Record<string, never>>;

/**
 * Side-effect only: defines `Set.prototype.union` when missing.
 * Import as: `import '@webreflection/utils/patch/set-union'`
 *
 * No ambient `Set` augmentation — modern `lib` already types `union`.
 * This entry is a runtime polyfill, not a types package.
 */
export {};
